/**
 * Build-time configurable anchor delimiter.
 * Used everywhere: line-hashing.ts, ReadFileToolHandler.ts, EditExecutor.ts, EditFormatter.ts.
 *
 * To change the delimiter, edit this constant and run `npm run build`.
 */
export const ANCHOR_DELIMITER = "|"
