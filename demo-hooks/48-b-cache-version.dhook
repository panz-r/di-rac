group("rust", ["src/**/*.rs"])

let session_check = validation(["cargo", "test", "--lib"])

@on("post_tool_use")
def validation():
    if changed_files.any_match("rust"):
        require(session_check)
