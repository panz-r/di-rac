group("rust", ["src/**/*.rs"])

@on("post_tool_use")
def validation():
    if changed_files.any_match("rust"):
        require_validation(argv=["cargo", "test", "--lib"], reason="Session override: faster Rust check")
